# Facts 想法类型

Core 1.3.45 / Desktop 1.1.76 / Schema 123。

`idea` 用于说话者提出的想法、构想、框架或思考模型。例如“建议试点订阅服务，尚未决定实施”。“已决定周五上线”仍是 commitment，“上周已上线”仍是 event。类型由模型判断，代码保留模型输出；不是用关键词强行重分类。

变化覆盖 Facts 抽取、数据库类型约束、Facts 文本围栏、类型筛选及置信度衰减。沿用上游 idea 的 365 天衰减参数。管理台显示“想法”，CLI recall 使用灯泡标识。Memory Verbs 与 Takes 自身的类型枚举不扩张。

可选配置 `facts.extraction_prompt_appendix` 用于添加运营者的抽取标准，默认空，不自动写配置。例如通过现有 config set 命令设置“会议中保留尚未决定和待确认语义；不要将提议改为已执行的决定”。此配置是受信任的操作配置，不从会话正文或源文件 frontmatter 自动加载。

迁移 123 放宽 facts.kind CHECK，保留旧五种类型和原记录；可重复执行。未自动迁移用户真实库，正常升级流程负责应用；没有历史重抽取或向量重建。旧记录中已经被存为 fact 的想法不会自动改类。

本地资源已重建：Admin 生产资源指纹 92a6950aac77；最新 Windows Sidecar 的 Bun、Canvas、PGLite 验证通过。当前源码页面使用隔离 API 响应核对“想法”筛选项，未连接真实知识库。抽取、衰减和文本围栏定向测试 65 项通过；迁移与历史向量保护 3 项通过、Postgres 1 项跳过。

补充验收：本机 Postgres 专用空测试库的四个相关文件共 6 项通过；PGLite/Facts/围栏 40 项通过。真实 Codex 用户原文 15 题使用本地 qwen3:4b 实测，预标注类别匹配 8/15，中文质量尚未达标，并发现当前桌面日志的用户消息漏读。详情及失败证据见 [Postgres 与真实中文评估](2026-09-08-Postgres与真实中文Facts评估.md)。GitHub、远程 CI 和打包由用户完成。

# PMBrain 桌面版安装与首次使用

本文面向第一次使用 PMBrain 的 Windows 用户，也适用于已经使用命令行版、Docker Postgres 或旧版 `.gbrain` 配置的升级用户。

## 先说结论

如果选择 **PGLite 零配置模式**，只需要安装最新的 `PMBrain-Windows-x64-Setup-*.exe`，不需要另外安装 Bun、Node.js、Docker 或 Postgres。安装包已经包含 PMBrain sidecar、PGLite 数据库和所需的 WASM 运行时。

如果选择 **Docker Postgres 模式**，PMBrain 安装包只负责连接数据库，不负责安装或启动 Docker。使用前需要自行准备可访问的 Postgres/pgvector 数据库。

## 安装前条件

### 所有用户

- Windows 10 1809 或更高版本，或 Windows 11；仅支持 64 位系统。
- x64 CPU 至少支持 SSE4.2。安装包内置 Bun x64 baseline，不要求 AVX2。
- 建议至少预留 1 GB 可用磁盘空间，知识库数据另计。
- 当前安装包尚未配置正式代码签名，Windows SmartScreen 可能显示未知发布者提示。
- 如果需要调用在线模型或自动更新，需要允许 PMBrain 访问对应模型服务和 GitHub。

### PGLite 模式

没有额外前置软件。数据库文件保存在本机，适合个人使用、首次体验和中小型知识库。

### Docker Postgres 模式

需要提前准备：

- Docker Desktop 或其他可用的 Postgres 服务。
- 已启用 `pgvector` 的数据库。
- 完整连接地址，例如 `postgresql://用户名:密码@127.0.0.1:5433/pmbrain`。
- 数据库服务已启动，并允许本机连接。

如果是第一次安装 PMBrain 且还没有准备数据库，请按 [PMBrain 新用户首次安装使用 Docker Postgres](./首次安装使用DockerPostgres.md) 操作。

## 新用户安装

1. 双击最新的 `PMBrain-Windows-x64-Setup-*.exe`。
2. 选择安装目录并完成安装。
3. 启动 PMBrain。只有本机不存在 PMBrain 配置时，才会出现首次配置向导。
4. 选择数据库模式：
   - 不想安装其他软件：选择 **PGLite 零配置**。
   - 已有 pgvector Postgres：选择 **Docker Postgres** 并填写连接地址。
5. 选择知识库目录。PMBrain 会将它注册为独立 Source，后续可以同步、归档和恢复。
6. 选择模型提供商并按需要填写 API Key；使用本地 OpenAI 兼容模型时，先点击“添加自定义接口”填写 Base URL。
7. 数据库初始化完成后，桌面端会进入 PMBrain 管理台。

初始化期间不要重复启动程序。首次创建 PGLite 数据库或执行迁移可能需要一些时间；点击“保存配置并启动”后请耐心等待页面提示完成。

保存配置前，桌面端会先校验内置 Bun 的版本、SHA-256、CPU/系统兼容性和 sidecar 版本。`3221225501` 对应 `0xC000001D`（非法 CPU 指令），不是数据库或 API Key 错误；遇到该错误时切换 Docker Postgres 无效，应先确认安装的是 Desktop 1.0.61 或更高版本。

## 模型 API Key 怎么填

API Key 不是启动 PMBrain 的硬性条件。不填写时仍可启动、管理 Source 和使用不依赖模型的能力，但向量化、对话、搜索扩展等 AI 功能会受到限制。

桌面向导支持 MIMO、智谱 AI、DeepSeek、OpenAI、Anthropic、ZeroEntropy、Ollama，以及自定义 OpenAI 兼容接口。可以先只填写一个当前可用的提供商，后续再从桌面设置补充。

已有 Key 的用户不需要重复填写。输入框显示“已配置，留空保留”时，留空不会删除原值。

## 本地 Qwen 模型怎么选

- 模型由 **Ollama** 部署：厂商直接选择 `ollama`，模型名称填写 Ollama 中实际存在的模型 ID。
- 模型由 **vLLM、LM Studio、Xinference、LocalAI** 或其他 OpenAI 兼容服务部署：分别点击普通模型或向量模型旁的“＋ 自定义模型”，填写供应商名称、当前模型的 Base URL 和模型 ID。两种模型可以连接不同端口。
- Base URL 必须是完整的 `http://` 或 `https://` 地址，并通常以 `/v1` 结尾，例如 `http://127.0.0.1:8000/v1`。如果服务在另一台电脑上，不能填写 `127.0.0.1`，应填写服务电脑的局域网 IP，并确保端口和防火墙允许访问。
- 模型名称必须与本地服务暴露的 ID 完全一致，例如 `Qwen3-35B-A3B` 或 `Qwen3-Embedding-8B`。本地服务没有鉴权时 API Key 可以留空。

点击页面“保存配置并启动”后，桌面端会把普通模型与向量模型各自的 Base URL、模型 ID 和可选 API Key 一次写入同一份本机配置；两类模型可以使用不同端口和不同 Key。旧版单一 `provider_base_urls.custom-openai` 地址与 `custom_openai_api_key` 继续作为兼容回退，已有配置无需迁移。首次使用新的向量模型时还会调用其独立地址下的 `/embeddings` 探测实际维度；如果服务未启动、URL 路径错误或模型 ID 不存在，页面会直接指出自定义向量模型验证失败，不再把数据库迁移日志当作主要错误展示。

## 更换向量模型后会发生什么

在桌面端修改向量模型并保存时，PMBrain 会先用新配置请求一次向量接口，确认模型可访问并读取真实维度。只有验证成功后，才会让旧向量失效并立即开始全量重算；即使新旧模型维度相同也会重算，避免把不同模型生成的向量混在同一个检索空间。这里只清理可再生成的向量和向量时间戳，不会删除原始页面、分块、知识内容或 Source。

如果网络、模型服务或程序退出导致本次没有全部完成，已经生成的新向量会保留，剩余分块继续保持待向量化状态。下一次正常运行 Dream 时，默认 `embed` 阶段会按 `embedding IS NULL` 继续处理剩余分块，不会从头重复已完成的部分。对于旧版本已经换过模型但没有重算的数据库，Dream 会先识别模型标识与当前配置不一致的非空向量，将它们转为待处理状态后自动补算。历史版本错误写入的 `zeroentropyai:zembed-1` 出厂标签属于 PMBrain 元数据 Bug：升级后会自动改成用户当前显式配置的向量模型，只修正标签，不清空或重建已有向量。CLI 用户使用 `pmbrain config set embedding_model <provider:model>` 时执行相同的验证、失效、立即重算和 Dream 续算规则。

## 老用户升级

桌面端按以下顺序读取本机配置：

1. `%USERPROFILE%\.pmbrain\config.json`
2. `%USERPROFILE%\.gbrain\config.json`（旧版兼容）

检测到任一现有配置后，PMBrain 会：

- 沿用数据库模式、数据库地址和模型 API Key。
- 沿用知识库目录和 MCP/API 凭证。
- 仅在桌面版本变化时执行一次幂等数据库迁移。
- 迁移成功后启动 sidecar、完成健康检查并直接进入管理台。

老用户正常启动时不需要经过配置向导，也不需要点击“保存配置并启动”。只有从 PMBrain 菜单主动选择“配置与 MCP 接入”时，才会进入设置页。

如果启动失败，桌面端会显示独立的恢复页。建议先点击“重新启动服务”或打开日志，不要重新填写已有配置。

## MCP 接入是否必须

不是。MCP 接入只用于让 CodeBuddy、Workbuddy、Cursor、Claude Code、Codex 或 QwenPaw 调用 PMBrain。

需要时进入“配置与 MCP 接入”页面：

- CodeBuddy、Workbuddy、Cursor、Codex：桌面端可以备份并合并配置。
- QwenPaw 2.x：先启动 QwenPaw，再点击一键接入。桌面端会通过 QwenPaw 本机 API 创建或更新 `pmbrain` DriverCard，把新凭证保存为完整的 `Authorization: Bearer ...`，并以 QwenPaw 实际返回的工具列表确认连接成功；不会触发 OAuth 授权。DriverCard 和凭证库写入前都会备份。
- 旧版 QwenPaw：没有 2.x 本机 API 时，桌面端兼容合并 `%USERPROFILE%\.qwenpaw\config.json` 的 `mcp.clients.pmbrain`，保留已有 MCP 和其他设置。
- Claude Code：桌面端生成可复制的接入命令。
- API Key 模式会执行 MCP smoke test。
- OAuth 模式会生成独立 client credentials。

## 自动更新

正式 GitHub Release 发布后，桌面端会在启动后自动检查更新：

1. 发现新版本后自动下载。
2. 下载完成后提示安装。
3. 安装前安全停止 sidecar。
4. 更新完成后重新启动 PMBrain。
5. 新版本先执行数据库迁移，再启动服务并进行健康检查。

自动更新不会覆盖现有数据库地址、API Key、知识库目录或 MCP 配置。

## 配置、数据、备份和日志位置

- 新版配置：`%USERPROFILE%\.pmbrain\config.json`
- 旧版兼容配置：`%USERPROFILE%\.gbrain\config.json`
- 配置备份：当前配置目录下的 `backups\config`
- MCP 配置备份：当前配置目录下的 `backups\mcp`
- 桌面日志：`%APPDATA%\PMBrain Desktop\logs`
- PGLite 数据：首次向导中显示或选择的数据库路径

卸载桌面程序不会主动删除上述用户配置和知识库数据。重新安装后可以继续读取。

## 常见问题

### 点击图标后没有窗口

先在任务管理器确认是否存在 `PMBrain.exe`。1.0.22 起，再次点击图标会显示、恢复或重建主窗口；如果旧版本遗留进程仍占用单实例锁，可以结束旧进程后重新启动。

### 提示 `Cannot find module @electric-sql/pglite/vector`

这是 1.0.21 安装包遗漏 PGLite 运行时造成的打包问题，与用户数据库配置无关。请安装 1.0.22 或更高版本。

### 老用户为什么看到配置页

正常升级应直接进入管理台。只有首次使用、用户主动打开设置，或服务启动失败时才会显示桌面壳。启动失败时应优先查看恢复页和日志。

### Docker Postgres 无法连接

确认容器正在运行、端口映射正确、连接地址可用，并确认数据库已安装 `pgvector`。PMBrain 不会擅自切换为 PGLite，也不会自动重建数据库。

### 没有 API Key 能不能用

可以启动和管理本地数据，但依赖模型的向量化、对话和搜索增强功能不可用。后续补充 Key 即可，不需要重新安装。

# 上游能力融合开发报告

## 本轮完成内容

本轮按 `PMBrain-local-upstream-fusion-plan.md` 先执行稳定性方向的第一项能力：移植 GBrain 的 DB 内容竞争 pacing 基础能力，并接入 PMBrain 的后台 stale embedding / embed-backfill 路径。

## 直接来源

- GBrain commit：`7968f8407709d42696efa60ecf0686b44dc5d4f5`
- 上游主题：`v0.42.49.0 feat(pace): native DB-contention pacing for embed/sync backfills`

## 文件处理

### 复制后适配

- `src/core/db-pacer.ts`
- `src/core/pace-mode.ts`
- `test/db-pacer.test.ts`
- `test/pace-mode.test.ts`

### 核心 diff 移植

- `src/core/embed-stale.ts`
- `src/core/minions/handlers/embed-backfill.ts`

## PMBrain 原有能力保留

- 未覆盖 `src/commands/sync.ts`、`src/commands/embed.ts`、`src/cli.ts`、Admin Console、MCP、Office 导入和数据库 Engine 核心文件。
- 保留 PMBrain 现有中文 CLI、国产模型、Office、桌面端、Tunnel 和项目管理定位。
- pacing 默认为 `off`，不会改变现有默认吞吐行为；只有配置或环境变量启用时生效。

## 当前使用方式

配置表可使用：

```text
pace.mode = gentle | balanced | aggressive | off
pace.max_concurrency = 4
pace.pace_at_ms = 500
pace.max_sleep_ms = 1500
pace.ewma_alpha = 0.3
```

环境变量可使用：

```powershell
$env:PMBRAIN_PACE_MODE="balanced"
$env:PMBRAIN_PACE_MAX_CONCURRENCY="8"
```

兼容旧变量：

```powershell
$env:GBRAIN_PACE_MODE="balanced"
```

## 测试结果

- `bun test test/db-pacer.test.ts test/pace-mode.test.ts`：30 pass / 0 fail。
- `bun run typecheck`：通过。

## 已知风险

- 本轮尚未把 pacing 接入 `sync.ts` 和 CLI `embed --stale` 的所有入口。
- Admin Console 仍未展示 pacing 状态。
- 后续要继续按计划推进 sync、supervisor、watchdog、spend posture、Skillpack、Advisor 等能力。

## 回滚方法

删除或回退以下文件改动即可回滚本轮功能：

- `src/core/db-pacer.ts`
- `src/core/pace-mode.ts`
- `src/core/embed-stale.ts`
- `src/core/minions/handlers/embed-backfill.ts`
- `test/db-pacer.test.ts`
- `test/pace-mode.test.ts`

并将 `VERSION`、`package.json` 版本号回退到上一版本。

## 2026-06-26 后台任务稳定性进展

### 已完成子项

- 移植 GBrain `bb2e88c42a4969e16df7a43a9eb118aa031e89a4` 的 minion 超时尝试次数修复。
- PMBrain 的 `handleTimeouts()` 现在会在把 active job 标记为 `dead` 时同步递增 `attempts_made`。
- 单元测试与 E2E 测试均增加断言，防止长任务超时后再次出现 `attempts: 0/N (started: N)` 的假象。

### 文件处理

- 核心 diff 移植：`src/core/minions/queue.ts`
- 测试 diff 移植：`test/minions.test.ts`、`test/e2e/minions-resilience.test.ts`
- 未复制上游独立文件：`src/core/minions/handler-timeouts.ts`，因为 PMBrain 当前实现已将该逻辑内联在 `queue.ts`。

### 当前验证

- `bun test --timeout 30000 test/minions.test.ts`：169 pass / 0 fail。
- `bun test --timeout 60000 test/e2e/minions-resilience.test.ts`：0 fail，因未设置 `DATABASE_URL` 按测试设计跳过 7 项。
- `bun run typecheck`：通过。

## 2026-06-26 后台任务稳定性进展二

### 已完成子项

- 移植 GBrain `bb2e88c42a4969e16df7a43a9eb118aa031e89a4` 的 live sync 状态与 progress-aware stall watchdog。
- `liveSyncStatus(engine, sourceId)` 可通过 per-source DB lock 判断该 source 是否有活跃同步持有者。
- `sync` 导入阶段增加无进度看门狗，默认 900 秒；配置优先读 `PMBRAIN_SYNC_STALL_ABORT_SECONDS`，兼容 `GBRAIN_SYNC_STALL_ABORT_SECONDS`；`<=0` 可关闭。
- stall 触发时会通过组合 AbortSignal 中止本轮导入，返回 `partial` 且 `reason='stall_timeout'`，不推进 `last_commit`。

### 文件处理

- 核心 diff 移植：`src/core/db-lock.ts`、`src/commands/sync.ts`
- 测试 diff/适配：`test/db-lock-inspect.test.ts`、`test/sync-stall-watchdog.test.ts`

### 当前验证

- `bun test test/sync-stall-watchdog.test.ts test/db-lock-inspect.test.ts`：20 pass / 0 fail。
- `bun run typecheck`：通过。

## 2026-06-26 后台任务稳定性进展三

### 已完成子项

- 移植 GBrain `bb2e88c42a4969e16df7a43a9eb118aa031e89a4` 的 supervisor degraded retry。
- `maxCrashes` 现在是软 crash 预算，超过后发出 `crash_budget_degraded` 并继续退避重启。
- 新增 hard stop 上限，默认 `maxCrashes * 10`；`PMBRAIN_SUPERVISOR_HARD_STOP_CRASHES=0` 可关闭永久自动停止。

### 文件处理

- 核心 diff 移植：`src/core/minions/child-worker-supervisor.ts`、`src/core/minions/supervisor.ts`
- 测试适配：`test/child-worker-supervisor.test.ts`、`test/supervisor.test.ts`

### 当前验证

- `bun run typecheck`：通过。
- `bun test test/supervisor.test.ts -t resolveHardStopMaxCrashes`：4 pass / 0 fail。
- `bun test --timeout 60000 test/child-worker-supervisor.test.ts test/supervisor.test.ts`：当前 Windows 环境下子进程/shell 型集成用例未正常跑起 worker，出现多项既有 worker_spawn/worker_exited 事件缺失；该结果记录为环境限制，后续需在具备 shell 子进程环境的 CI 或类 Unix 环境复测。

## 2026-06-26 JSONB / checkpoint 可靠性进展

### 已完成子项
- 参考 GBrain `9bf96db807c2f050449142f2f0b05726f58e5054`，移植 `op_checkpoints.completed_keys` 数组形态保护。
- fresh schema、PGLite schema、embedded schema 均新增 `op_checkpoints_completed_keys_array` 命名 CHECK。
- migration v108 会把历史非数组 `completed_keys` 修复为 `[]`，再添加约束。
- `loadOpCheckpoint()` 对非数组值走专门 warning 路径，不再让 JSON parse failure 混淆真实原因。

### 文件处理
- 核心 diff 移植：`src/core/op-checkpoint.ts`、`src/core/migrate.ts`
- schema 同步：`src/schema.sql`、`src/core/pglite-schema.ts`、`src/core/schema-embedded.ts`
- 测试适配：`test/op-checkpoint.test.ts`

### 当前验证
- `bun test --timeout 30000 test/op-checkpoint.test.ts`：23 pass / 0 fail。
- `bun run typecheck`：通过。

### 已知限制
- PMBrain 当前没有上游 `op_checkpoint_paths` 子表，本轮没有移植上游 append-only checkpoint UNION 读取逻辑。
- 上游同一提交中的 page generation sequence swap、doctor freshness 展示和 break-lock 诊断尚未移植，后续需拆成独立子项。

## 2026-06-26 spend posture 成本控制进展

### 已完成子项
- 新增 `src/core/spend-posture.ts`，提供统一的 `spend.posture` 解析和 USD limit 解析。
- `spend.posture=tokenmax` 被解析为成本门控的 informational posture；未知值默认回到 `gated`，避免误关门控。
- `off`、`unlimited`、`none` 解析为 `Infinity`，输出时格式化为 `unlimited`，避免 JSON 把 Infinity 序列化成 `null`。
- `pmbrain config set spend.posture` 会校验合法值；相关 spend-control 配置键进入 `KNOWN_CONFIG_KEYS`。

### 文件处理
- 复制后适配：`src/core/spend-posture.ts`
- 核心 diff 移植：`src/core/config.ts`、`src/commands/config.ts`
- 测试适配：`test/spend-posture.test.ts`、`test/config-set.test.ts`

### 当前验证
- `bun test --timeout 30000 test/spend-posture.test.ts test/config-set.test.ts`：26 pass / 0 fail。
- `bun run typecheck`：通过。

### 已知限制
- 本轮没有接入 sync 的 delta-aware cost estimator，也没有改变现有 embedding/backfill 的实际执行路径。
- `spend.posture=tokenmax` 当前作为基础配置可设置和读取；具体门控绕过/提示行为需后续在各命令独立接线。

## 2026-06-26 Skillpack + Advisor 进展

### 已完成子项
- 新增 `pmbrain advisor` 和 `pmbrain advisor --json`。
- Advisor 当前只读，不自动执行修复命令。
- 已接入的 collector：pending migration、schema pack resolvability、stalled jobs、stale sync、embedding/setup smells、usage health。
- finding 会按 critical / warn / info 排序，并保留 agent-readable 修复命令。

### 文件处理
- 算法复制+PMBrain Adapter：`src/core/advisor/*`
- CLI 接入：`src/commands/advisor.ts`、`src/cli.ts`
- 测试适配：`test/advisor-core.test.ts`

### 当前验证
- `bun test --timeout 30000 test/advisor-core.test.ts`：7 pass / 0 fail。
- `bun run typecheck`：通过。

### 已知限制
- 本轮没有移植 `--apply`，避免自动执行命令风险。
- 本轮没有接入 brain-resident skillpack collector，因为 PMBrain 还缺上游 `brain-resident-locate` / `nag-state` 等依赖。
- 本轮没有改 Admin Console，因此不提供管理员链接。

## 2026-06-26 Git Source Durability 进展

### 已完成子项
- 新增 `pmbrain sources harden <id|--all>`，仅对已有本地 Git Source 生效，会安装 local post-commit hook、写入 committed helper、写入 resolver/AGENTS durability 规则，并可选配置 repo-scoped PAT credential。
- 新增 `pmbrain sources pull <id>` 和 `pmbrain sources pull --path <dir>`；其中 `--path` 形式在 CLI 层提前执行，不连接数据库，适合定时任务或外部调度。
- 新增 `pmbrain sources unharden <id>`，移除本地 hook、credential wiring 和 cron/调度残留；已提交到知识库仓库的 helper/rules 不会被自动删除。
- `git-remote.ts` 增加 divergence-safe pull、default branch detection、push probe、dirty tree 检测；保留原有 clone/pull SSRF 防护和 `pullRepo --ff-only` 行为。
- Windows 上不调用 `crontab`，cron 安装返回 skipped；后续如需系统级定时任务，应独立实现 Task Scheduler 适配。

### 文件处理
- 复制后适配：`src/core/brain-repo-durability.ts`、`src/commands/sources-harden.ts`
- 核心 diff 移植：`src/core/git-remote.ts`、`src/commands/sources.ts`、`src/cli.ts`
- 测试适配：`test/brain-repo-durability.test.ts`、`test/git-remote-durable.serial.test.ts`

### 当前验证
- `bun test --timeout 30000 test/git-remote-durable.serial.test.ts test/brain-repo-durability.test.ts`：10 pass / 0 fail。
- `bun run typecheck`：通过。

### 已知限制
- 尚未把 harden 自动接入 `sources add --url --pat-file`，避免本轮改变新增 Source 的默认副作用。
- 尚未修改 Admin Console、Desktop 或 MCP 暴露面。
- Windows 系统级定时 pull 只提供 DB-free CLI 能力，未安装 Task Scheduler。

## 2026-06-26 Federated Read Scope 进展

### 已完成子项
- `getPage()` 支持 `sourceIds[]` 联邦读取范围，`get_page` 精确读取不再只按标量 `ctx.sourceId`。
- `get_tags`、`get_links`、`get_backlinks`、`get_timeline` 均改为接收 `sourceScopeOpts(ctx)` 或 link 专用 scope。
- link 联邦读取分支同时过滤 from、to、origin 三个页面端点；origin 不在授权范围内时不暴露 slug。
- 远程调用只有标量 sourceId 时，link 读取提升到 `{ sourceIds: [sourceId] }`，本地可信调用仍保留旧的标量跨源视图。

### 文件处理
- 核心 diff 移植：`src/core/operations.ts`、`src/core/types.ts`、`src/core/engine.ts`
- Engine 查询适配：`src/core/pglite-engine.ts`、`src/core/postgres-engine.ts`
- 测试适配：`test/get-page-federated-scope.test.ts`

### 当前验证
- `bun test --timeout 60000 test/get-page-federated-scope.test.ts`：5 pass / 0 fail。

### 已知限制
- 本轮未修改 Admin Console、Desktop 或 MCP 工具定义，只修复底层 operation/engine 读取范围。
- Postgres 分支通过类型检查覆盖 SQL 构造形状，专项正向执行测试仍以 PGLite 为主。

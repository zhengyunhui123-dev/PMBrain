# 上游能力移植审计

## 本轮范围

- 执行计划步骤：第二步 `db-pacer + pace-mode` 的最小可验证落地。
- 移植类型：A 类直接复制后适配 + C 类核心 diff 移植。
- 修改边界：只修改 `D:\cursor-claude\PMBrain`，`D:\cursor-claude\gbrain` 只读。

## 双仓库基线

- PMBrain HEAD：`68f27a9bdcc27371d2a965f91b6efc7f0f98a452`
- GBrain HEAD：`bb2e88c42a4969e16df7a43a9eb118aa031e89a4`
- 本轮上游来源提交：`7968f8407709d42696efa60ecf0686b44dc5d4f5`
- PMBrain 现有未跟踪文件保留：`dream-cycle-summaries/`、`run-worker.cmd`、`项目管理/PMBrain-local-upstream-fusion-plan.md`

## 分类结果

### 可以复制后适配

- `src/core/db-pacer.ts`
- `src/core/pace-mode.ts`
- `test/db-pacer.test.ts`
- `test/pace-mode.test.ts`

适配点：

- PMBrain 没有上游 `abort-check.ts`，因此 `AbortError` 改为由 `db-pacer.ts` 本地导出。
- 环境变量解析改为 `PMBRAIN_PACE_*` 优先，兼容 `GBRAIN_PACE_*`。

### 只移植 diff

- `src/core/embed-stale.ts`
- `src/core/minions/handlers/embed-backfill.ts`

移植内容：

- stale chunk 查询、读 chunk、写 chunk 的 DB 操作接入 `observed(pacer, ...)`。
- 每个 slug-key 完成后调用 `pacer.pace(signal)`，取消时安全退出。
- embed-backfill handler 从配置和环境解析 pace bundle，传入 stale embedding 循环，并在 finally 中 dispose。

### 本轮未采用

- `src/commands/embed.ts` 的 CLI `--pace` 和 single-flight 全量接入。
- `src/commands/sync.ts` 的多 worker shared pacer。
- `src/core/embed-backfill-lock.ts` 独立锁文件。

原因：本轮先做稳定性最小闭环，避免一次性扩大到 CLI、sync 和 job payload 行为。

## 后台任务稳定性组 - minion 超时尝试次数

- 执行计划步骤：第三步 `数据库连接、supervisor、cooldown、watchdog` 中的 minion 超时可靠性子项。
- 移植类型：C 类核心 diff 移植。
- 上游来源：GBrain `bb2e88c42a4969e16df7a43a9eb118aa031e89a4`，#1737 `handleTimeouts counts the timed-out run as a spent attempt`。
- PMBrain 现状：PMBrain 没有独立 `src/core/minions/handler-timeouts.ts`，对应逻辑已内联在 `src/core/minions/queue.ts`。
- 采用内容：在 `handleTimeouts()` 的 dead-letter SQL 中补充 `attempts_made = attempts_made + 1`，并补充 `test/minions.test.ts` 与 `test/e2e/minions-resilience.test.ts` 断言。
- 未采用内容：未复制上游 `handler-timeouts.ts` 文件，避免与 PMBrain 现有队列实现产生第二套超时路径。

## 后台任务稳定性组 - live sync 状态与停滞看门狗

- 执行计划步骤：第三步 `数据库连接、supervisor、cooldown、watchdog` 中的 sync running 可见性与 stall watchdog 子项。
- 移植类型：C 类核心 diff 移植。
- 上游来源：GBrain `bb2e88c42a4969e16df7a43a9eb118aa031e89a4`，#1950 `honest live-sync status + progress-aware stall-abort`。
- 采用内容：`src/core/db-lock.ts` 新增 `liveSyncStatus()`；`src/commands/sync.ts` 新增 `resolveStallAbortSeconds()`、`composeAbortSignals()`，并在导入阶段无文件完成超过阈值时 abort，返回 `stall_timeout`。
- PMBrain 适配：环境变量使用 `PMBRAIN_SYNC_STALL_ABORT_SECONDS` 优先，兼容 `GBRAIN_SYNC_STALL_ABORT_SECONDS`；PMBrain 当前无 `test/sync-hard-deadline.test.ts`，因此新增 `test/sync-stall-watchdog.test.ts` 覆盖纯 helper。
- 未采用内容：暂未把 `liveSyncStatus()` 接入 `sources status` 或 Admin 状态页，避免在本子项中扩大到产品展示层。

## 后台任务稳定性组 - supervisor degraded retry

- 执行计划步骤：第三步 `数据库连接、supervisor、cooldown、watchdog` 中的 supervisor 自愈子项。
- 移植类型：C 类核心 diff 移植。
- 上游来源：GBrain `bb2e88c42a4969e16df7a43a9eb118aa031e89a4`，#1994 `degraded retry instead of permanent give-up on crash storm`。
- 采用内容：`ChildWorkerSupervisor` 将 `maxCrashes` 改为软预算，超过后发出 `crash_budget_degraded` 并继续退避重试；新增 `hardStopMaxCrashes` 作为永久停止硬上限；`MinionSupervisor` 增加 hard stop 解析并透传给子 supervisor。
- PMBrain 适配：环境变量使用 `PMBRAIN_SUPERVISOR_HARD_STOP_CRASHES` 优先，兼容 `GBRAIN_SUPERVISOR_HARD_STOP_CRASHES`。
- 已知限制：本轮暂未移植 supervisor status/doctor 的 DB-lock fallback 展示层。

## JSONB / checkpoint 组 - op_checkpoints 数组形态保护

- 执行计划步骤：第四步 `JSONB、checkpoint、中断恢复` 中的 checkpoint integrity 子项。
- 移植类型：C 类核心 diff 移植。
- 上游来源：GBrain `9bf96db807c2f050449142f2f0b05726f58e5054` 中 `op_checkpoints array-shape guard` 修复。
- PMBrain 适配：PMBrain 当前没有上游 `op_checkpoint_paths` append-only 子表，因此没有移植上游的 UNION 子表读取；只保留父表 `completed_keys` 数组约束、迁移修复和 loader 防御。
- 采用内容：fresh schema 三处新增 `op_checkpoints_completed_keys_array` 命名约束；migration v108 修复历史非数组值并加约束；`loadOpCheckpoint()` 先读取 `jsonb_typeof`，非数组时输出 warning 并返回空进度。
- 未采用内容：上游 page_generation_clock sequence swap、doctor sync freshness 展示和 `--force-break-lock` 诊断不在本子项范围内，需后续单独审计。

## spend posture 组 - 基础配置原语

- 执行计划步骤：第五步 `spend posture 成本控制` 的基础层。
- 移植类型：A 类复制后适配。
- 上游来源：GBrain `2e1089050282e8a33616578ffef3a1c6809a5f22` 的 `spend-posture.ts`，以及 `1bb3b7a7fb84e1092e12e835851b542e5d4c2d4e` 的 config 注册与校验。
- PMBrain 适配：提示文案使用 `pmbrain config set ...`；只新增 leaf module，不触碰现有 sync/enrich/reindex 执行路径。
- 采用内容：`resolveSpendPosture()`、`normalizeSpendPosture()`、`isValidSpendPosture()`、`parseUsdLimit()`、`formatUsdLimit()`、`usdLimitToCap()`；`KNOWN_CONFIG_KEYS` 注册 spend-control 键；`config set spend.posture` 拒绝非法值。
- 未采用内容：上游 #2139 的 delta-aware sync cost estimator、non-TTY auto-defer、reindex/enrich/onboard gate 接线尚未移植。

## Skillpack + Advisor 组 - Advisor 只读最小闭环

- 执行计划步骤：第六步 `Skillpack + Advisor` 的 Advisor 子项。
- 移植类型：B 类算法复制 + PMBrain Adapter。
- 上游来源：GBrain `eebbfc2e` / `9d88680a` 的 `src/core/advisor/*` 与 `src/commands/advisor.ts`。
- PMBrain 适配：命令名、fix 提示和渲染文案改为 `pmbrain`；去掉本轮尚未移植依赖的 brain-resident skillpack collector、workspace installed-skills collector、history 和 `--apply`。
- 采用内容：advisor finding/report 类型、collector 排序和 resilience、migration/schema-pack/stalled-jobs/setup-smells/usage-shape collector、human/JSON 输出、CLI exit code。
- 未采用内容：`list_brain_skillpack` MCP tool、`gbrain advisor --apply`、Admin 首页建议、brain-resident source skillpack 发现与 nag 状态；这些需在后续 Skillpack 子项继续移植。

## Git Source Durability 组
- 执行计划步骤：第八步 `Git Source Durability`。
- 移植类型：B 类算法复制 + PMBrain Adapter；`src/core/git-remote.ts` 为 C 类核心 diff 移植。
- 上游来源：GBrain `7ea92d602c4fe8a7070dfc8fa5e86f88c8c4fa71`。
- 采用内容：`divergenceSafePull()`、`detectDefaultBranch()`、`pushProbe()`、`isWorkingTreeDirty()`、repo-scoped credential、local post-commit hook、committed `scripts/brain-commit-push.sh` helper、resolver/AGENTS durability block、DB-free `sources pull --path`。
- PMBrain 适配：命令和文案改为 `pmbrain`；`PMBRAIN_HOME`、`PMBRAIN_GITHUB_PAT`、`PMBRAIN_GIT_ALLOW_FILE_TRANSPORT` 优先，兼容 `GBRAIN_HOME`、`GBRAIN_GITHUB_PAT`、`GBRAIN_GIT_ALLOW_FILE_TRANSPORT`；Windows 平台明确跳过 cron 安装/移除，避免调用 `crontab`。
- 未采用内容：`sources add --url --pat-file` 的自动 harden、`--no-harden`、remove 前自动 unharden、完整 OS 级 Windows Task Scheduler 安装器；这些会改变现有 add/remove 行为，留到后续独立审计。

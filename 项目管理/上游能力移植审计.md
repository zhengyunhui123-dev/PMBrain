# 上游能力移植审计

## 本轮范围

- 执行计划步骤：第二步 `db-pacer + pace-mode` 的最小可验证落地。
- 移植类型：A 类直接复制后适配 + C 类核心 diff 移植。
- 修改边界：只修改 `D:\cursor-claude\PMBrain`，`D:\cursor-claude\gbrain` 只读。

## 双仓库基线

- PMBrain HEAD：`68f27a9bdcc27371d2a965f91b6efc7f0f98a452`
- GBrain HEAD：`bb2e88c42a4969e16df7a43a9eb118aa031e89a4`
- 本轮上游来源提交：`7968f8407709d42696efa60ecf0686b44dc5d4f5`
- PMBrain 现有未跟踪文件保留：`dream-cycle-summaries/`、`run-worker.cmd`、`项目管理/PMBrain-local-upstream-fusion-plan.md`

## 分类结果

### 可以复制后适配

- `src/core/db-pacer.ts`
- `src/core/pace-mode.ts`
- `test/db-pacer.test.ts`
- `test/pace-mode.test.ts`

适配点：

- PMBrain 没有上游 `abort-check.ts`，因此 `AbortError` 改为由 `db-pacer.ts` 本地导出。
- 环境变量解析改为 `PMBRAIN_PACE_*` 优先，兼容 `GBRAIN_PACE_*`。

### 只移植 diff

- `src/core/embed-stale.ts`
- `src/core/minions/handlers/embed-backfill.ts`

移植内容：

- stale chunk 查询、读 chunk、写 chunk 的 DB 操作接入 `observed(pacer, ...)`。
- 每个 slug-key 完成后调用 `pacer.pace(signal)`，取消时安全退出。
- embed-backfill handler 从配置和环境解析 pace bundle，传入 stale embedding 循环，并在 finally 中 dispose。

### 本轮未采用

- `src/commands/embed.ts` 的 CLI `--pace` 和 single-flight 全量接入。
- `src/commands/sync.ts` 的多 worker shared pacer。
- `src/core/embed-backfill-lock.ts` 独立锁文件。

原因：本轮先做稳定性最小闭环，避免一次性扩大到 CLI、sync 和 job payload 行为。

## 后台任务稳定性组 - minion 超时尝试次数

- 执行计划步骤：第三步 `数据库连接、supervisor、cooldown、watchdog` 中的 minion 超时可靠性子项。
- 移植类型：C 类核心 diff 移植。
- 上游来源：GBrain `bb2e88c42a4969e16df7a43a9eb118aa031e89a4`，#1737 `handleTimeouts counts the timed-out run as a spent attempt`。
- PMBrain 现状：PMBrain 没有独立 `src/core/minions/handler-timeouts.ts`，对应逻辑已内联在 `src/core/minions/queue.ts`。
- 采用内容：在 `handleTimeouts()` 的 dead-letter SQL 中补充 `attempts_made = attempts_made + 1`，并补充 `test/minions.test.ts` 与 `test/e2e/minions-resilience.test.ts` 断言。
- 未采用内容：未复制上游 `handler-timeouts.ts` 文件，避免与 PMBrain 现有队列实现产生第二套超时路径。

## 后台任务稳定性组 - live sync 状态与停滞看门狗

- 执行计划步骤：第三步 `数据库连接、supervisor、cooldown、watchdog` 中的 sync running 可见性与 stall watchdog 子项。
- 移植类型：C 类核心 diff 移植。
- 上游来源：GBrain `bb2e88c42a4969e16df7a43a9eb118aa031e89a4`，#1950 `honest live-sync status + progress-aware stall-abort`。
- 采用内容：`src/core/db-lock.ts` 新增 `liveSyncStatus()`；`src/commands/sync.ts` 新增 `resolveStallAbortSeconds()`、`composeAbortSignals()`，并在导入阶段无文件完成超过阈值时 abort，返回 `stall_timeout`。
- PMBrain 适配：环境变量使用 `PMBRAIN_SYNC_STALL_ABORT_SECONDS` 优先，兼容 `GBRAIN_SYNC_STALL_ABORT_SECONDS`；PMBrain 当前无 `test/sync-hard-deadline.test.ts`，因此新增 `test/sync-stall-watchdog.test.ts` 覆盖纯 helper。
- 未采用内容：暂未把 `liveSyncStatus()` 接入 `sources status` 或 Admin 状态页，避免在本子项中扩大到产品展示层。

{
  "name": "pmbrain-desktop",
  "productName": "PMBrain Desktop",
  "desktopName": "PMBrain.desktop",
  "version": "1.1.53",
  "private": true,
  "author": "PMBrain",
  "description": "PMBrain cross-platform desktop shell",
  "main": "out/main/index.js",
  "type": "module",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "build:sidecar": "bun run scripts/build-sidecar.ts",
    "build:dir": "electron-builder --win dir --x64 --publish never",
    "build:win": "bun run ../scripts/check-version-sync.ts && bun run build && bun run build:sidecar && electron-builder --win nsis --x64 && bun run verify:package",
    "build:mac": "bun run build && bun run build:sidecar && electron-builder --mac dmg zip --arm64 --publish never && bun run verify:package --platform=darwin",
    "build:linux": "bun run build && bun run build:sidecar && electron-builder --linux AppImage --x64 --publish never && bun run verify:package --platform=linux",
    "preview:renderer": "bun run scripts/preview-renderer.ts",
    "verify:runtime": "bun run scripts/verify-runtime.ts",
    "verify:package": "bun run scripts/verify-package.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "electron": "^36.5.0",
    "electron-builder": "^26.0.12",
    "electron-vite": "^3.1.0",
    "jszip": "^3.10.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "zod": "^4.3.6"
  }
}

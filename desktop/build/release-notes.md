## PMBrain 1.1.46

- MCP 接入将 Trae 改为 Trae Work，写入 `%APPDATA%\TRAE SOLO CN\User\mcp.json`，不再写入已停用的 `Trae\User\mcp.json`。

## PMBrain 1.1.45

- 自动升级备份只保留最近 2 份，升级成功后清理更早备份，避免最新一份本身有问题时无法回退。
- 软件修复页显示备份份数和占用空间，支持恢复此备份、删除此备份、打开备份目录、清理旧备份，以及把备份位置改到 D 盘等其他磁盘。

## PMBrain 1.1.44

- 首次配置探测向量模型时不再对 `models` 命令跳过数据库关闭；只有导入、向量化和整理这类大批量写库命令才会在完成后直接退出，避免打包后探测失败。
- 任务中心在导入已打印完成后若子进程不退出，会按 stderr 里的导入结果收尾，避免 PDF 导入成功却一直显示执行中。

## PMBrain 1.1.43

- 提交前增加 GitHub Test 预演：漏改的检索契约会被拦住，Windows 会查找 Git Bash，不能再用近似步骤代替 verify。
- 核心用户路径改为启动打包后的 `PMBrain.exe`，导入完成以任务“已完成”为准，用来覆盖“别人打不开、导入后搜不到”的发布风险。

## PMBrain 1.1.42

- 修复 Ollama 本地普通模型在 AI 搜索中检索到知识却返回空答案的问题。知识综合任务会为本地模型保留 8192 上下文并限制结构化回答预算，避免 4096 默认窗口把检索证据截断；Qwen3 使用完整回答 Schema，空答案不再误报“已完成”。云端普通模型调用不受影响。
- 加固向量安全合同：桌面升级、启动、普通模型同步、Dream 和普通向量补全均不得静默切换用户向量模型或清空重建；只有用户明确确认真实换模才允许进入重建路径，空向量库只允许 `--empty-only` 对齐。

## PMBrain 1.1.41

- 新增历史向量配置安全恢复：当当前 ZeroEntropy 配置缺少密钥、同一数据库的配置备份记录了原向量模型时，桌面端提供无重建恢复。恢复前会校验实际向量维度和模型标签；仅校正历史误标，任何不匹配都会拒绝并回滚配置，不清空或重新生成已有向量。真实模型切换仍需明确确认并重建。

## PMBrain 1.1.40

- 修复桌面升级为同步普通模型而启动完整 CLI、可能产生无关数据库与向量副作用的问题。升级现在只在配置文件中更新 `chat_model` 和 `models.default`，不会改变用户选择的向量模型、维度、向量列、供应商密钥或已有向量；普通 CLI 连接也不再自动改写历史向量的模型标签。

## PMBrain 1.1.39

- 修复 Ollama 本地普通模型在 AI 搜索和深度整理中长时间输出思考内容、最终超时或意图为空的问题。普通 Ollama 问答改用原生流式接口，Qwen3 关闭思考并保留业务 JSON 输出；云端模型调用不受影响。

## PMBrain 1.1.38

- 修复升级后旧 Sidecar 仍占用 PGLite 时只能反复重试的问题。恢复页会识别经过身份校验的 PMBrain 占用进程，提供“结束占用进程并重启”操作；不会结束未知进程，也不会删除数据库、知识内容或锁文件。

## PMBrain 1.1.37

- 修复切换向量模型后桌面端同步等待完整重建导致配置界面卡住的问题。恢复本地服务后，重新向量化交给带 PGLite 锁交接和自动重连的任务中心后台任务执行。

## PMBrain 1.1.36

- 修复桌面端保存普通模型或 API Key 时重复执行 `sources add/default` 导致路径重叠报错的问题。主源路径兼容补齐改为一次性、可持久化的历史修复；冲突时安全跳过，用户主动切换到无路径主源时保留其选择。

## PMBrain 1.1.35

- 修复快速维护运行期间任务中心误显示 PGLite 残留占用进程恢复提示的问题；只有没有实际运行任务但数据库仍被占用时才显示恢复卡片。

## PMBrain 1.1.28

- 修复首次未配置向量模型、之后再配置模型时数据库向量列仍停留在旧维度的问题。桌面启动会先读取实际列维度和已有向量数，仅对空向量库安全对齐；已有向量不会被自动清空，需用户明确确认模型重建。

## PMBrain 1.1.27

- 自定义模型可以添加多个：普通模型和向量模型分开保存，下拉列表里显示已添加的供应商，也可以在下拉里点 × 删除。
- 知识库状态的“本次新增”改为按实际写入的页面计数，不再把检测到但没写进去的文件算成新增。
- 数据源页的 Git 仓库“上次同步”改为显示最近一次本地 Git 提交时间。

## PMBrain 1.1.26

- 对齐 GBrain 的 MCP Skill 用法：Agent 可通过 `list_skills` / `get_skill` 先取写作与工作流说明，再搜索知识。本地服务默认打开 Skill 发布。WorkBuddy 的 Agent 写入也会提示先读 Skill。

## PMBrain 1.1.25

- WorkBuddy 卡片恢复为普通 MCP 接入样式，不再做工作目录深度接入。已配置时「更新」旁边增加「Agent写入」，把用户级 `@pmbrain` 子代理和 `/pmbrain` 命令写到客户端用户目录。

## PMBrain 1.1.24

- 修复知识库「事实」页有计数却列表空白：Postgres 把事实编号当成字符串时契约校验失败，错误还被前端吞掉。现在会正常列出事实，失败时显示错误而不是沿用上一页的条数。

## PMBrain 1.1.23

- 修复已有知识库升级后 Sidecar 因缺少 `links_extracted_at` 列而无法启动的问题。启动时会自动补列，不再要求先手动跑迁移。

## PMBrain 1.1.22

- 对齐 GBrain 关系抽取水位：新增 `pages.links_extracted_at`，支持 `pmbrain extract --stale` 和医生检查“有多少页关系过期”。
- 知识库补上公司、事件页分类；事实详情显示事件类型。
- Agent 可通过 `entity` 零模型查看人物/公司/项目卡片。

## PMBrain 1.1.21

- 对齐原版 GBrain 的 fact 写入能力：Agent 可通过 `remember` 记住一条事实，通过 `forget` 忘掉，并通过 `recall` 读回。
- 知识库新增「事实」页，直接展示数据库 `facts` 表；知识页详情也会显示挂在该页上的事实。
- 知识分类调整：笔记、人物、项目归入结构化知识；原始与资料只保留会话、会议、导入材料和参考来源。

## PMBrain 1.1.20

- 新增 WorkBuddy 深度接入 V1：选择工作目录后，将 PMBrain Agent Rules 和 5 个官方 Skills 安装到 WorkBuddy 实际读取的 `.codebuddy` workspace 目录，并继续复用现有 MCP 连接。
- 支持安装状态检查、安全更新和卸载；用户修改过的 Rules/Skills 不会被静默覆盖或删除，其他 WorkBuddy MCP 服务保持不变。
- WorkBuddy 可主动检索 PMBrain、按明确指令记忆与纠错、选择性持久写回，并查看、接受或拒绝 AI 深度整理产生的待审核观点。

## PMBrain 1.1.19

- 新增轻量知识图谱：支持局部逐层探索、全局概览、孤立页审计、搜索、Source/关系筛选、悬停聚焦、方向显示、缩放拖动、全屏查看和知识详情。
- 修复普通相对 Markdown 链接在 Windows 路径和非默认 Source 中未形成关系的问题；快速维护一次补全当前 Source 的历史确定性关系，并正确保存 PostgreSQL 断点。
- 关联补全支持实体、概念、`知识点-标题` 和显式别名；同 Source 同名时停止猜测，显式链接优先并清理重复或失效的推断关系。

## PMBrain 1.1.18

- 首次使用向导的原始资料目录默认留空；选择目录后显示主源名称、路径和 Git 自动同步状态。
- 只有点击「启用 Git 自动同步」后才会初始化 Git，选择目录不会隐式创建仓库。

## PMBrain 1.1.17

- 基础配置数据库选择补充说明：PGLite 适合单机自用（单写者，不适合多人并发）；多用户/局域网共享建议使用 Docker Postgres。
- 系统设置开启「共享模式」且当前为 PGLite 时显示风险提示；保存共享成功时也会提醒可能影响体验。

## PMBrain 1.1.16

- 升级后首次启动会在冷备完成后自动等待并最多重试 3 次拉起本地服务，成功后直接进入管理台，减少手点「重新启动服务」。
- 升级冷备若打印成功结果但进程退出码异常，仍按成功处理；恢复页不再把冷备 JSON 当成失败原因。

## PMBrain 1.1.15

- 管理台移除已经失去作用的“导入与向量化”阈值设置；普通 Markdown、MCP 和外部输入仍由核心 500KB 内容安全规则保护，可信结构化 Office/PDF 继续按 Section → Chunk 处理；Admin 内嵌资源生成不再写入动态日期，避免跨 UTC 零点时被 CI 误判为生成物过期。
- 修复 PGLite 一次性命令已经完成但数据库关闭句柄卡住时，首次配置或 Admin 长时间停留在“处理中”的问题；Windows CI 现在会真实验证打包 Sidecar 连续上传 Markdown 和 PDF。
- 修复不同 Windows 构建环境生成 Admin HTML 空行不一致的问题，并同步更新只验证 Windows Desktop Runtime 的 CI 契约。
- 可信的大型 PDF、Word、PPT 和 Excel 文档不再因结构化正文超过 500KB 而整篇跳过向量化；普通 Markdown、MCP、Webhook 和远程输入仍保留原安全限制。
- 大文档按章节生成带文档名、章节路径和页码等定位信息的 Chunk，分批持久化向量；中断后可从已完成 Chunk 继续，仅修改章节时复用未变化的向量。
- 文档导入升级为本地结构化解析：PDF、Word、PPT 和 Excel 统一保留章节、列表、表格及页码、幻灯片、工作表定位。
- PDF 优先使用固定版本的本地 Rust 解析器，异常时自动回退到原有 `pdf-parse`，不会因结构化升级阻断既有导入。
- 导入页新增“结构化解析”和可选“图片内容识别”开关，并明确本地解析与可能调用视觉模型的边界。
- Windows Sidecar 内置并校验 PDF 原生库；修复打包环境 Excel 文件读取失败及无 BOM UTF-8 CSV 中文乱码。
- 新增结构化文档 V1/V2 检索 Benchmark 框架；真实私有文档语料与人工标注仍需在发布前补齐并运行。

## PMBrain 1.1.14

- 修复软件修复页“导出诊断包”按钮文字不可见的问题，保持与其他主操作按钮一致。
- 加强诊断包脱敏：管理员登录链接、未配置为主目录的本地绝对路径也不会导出。
- 诊断包补充 Dream 运行状态和完整 Sidecar 日志，并明确统计数据只代表导出时实际运行的数据库引擎。

## PMBrain 1.1.13

- Admin 的知识库、Dream、导入、模型与 Source 接口采用共享 Zod 合同，前后端字段不一致会在测试或接口边界直接报错。
- 新增可脱敏的一键诊断包，包含版本、Doctor、数据库/模型/MCP/更新状态和近期日志，不包含知识库或数据库文件。
- 发布流程新增统一 release manifest 与 Admin 生成物一致性检查；新增 100 问中文项目知识检索基准。
- 修复后台任务停滞扫描可能重入和 Markdown 摘要截断 Unicode 代理对的问题，保留现有数据库架构与 GBrain 环境变量兼容。

## PMBrain 1.1.12

- 新增真实 Desktop/Admin/PGLite/MCP 核心用户路径 E2E，并在发布后用上一版 NSIS 验证自动更新和数据保留。
- 修复 Console 拆页后导入结果和知识详情页缺少 Markdown 渲染组件导致的运行时白屏。

- 优化桌面主进程结构：数据库、备份、Sidecar、局域网、更新、窗口、托盘、系统设置、集成、首次配置、IPC 与窗口信任边界分别迁移到独立产品模块，入口只保留生命周期编排。
- 保持数据库、Sidecar、更新与现有用户配置行为不变。

## PMBrain 1.1.10

PMbrain桌面版本周更新汇总：

1. 设置中新增「普通模型调用」全局开关（默认关闭）。关闭后不再调用 DeepSeek 等聊天模型，向量化、语义搜索、混合搜索和快速维护仍可正常使用；开启后才可进行 AI 深度整理和 AI 会议整理。
2. 知识工作台「搜索」支持关键词搜索与语义搜索切换，默认关键词搜索，不依赖普通模型；「发送」仍用于 AI 意图处理。
3. 定时一键整理改为与「快速维护」同一套逻辑，每天自动维护更轻量，避免后台误跑深度整理消耗模型额度。
4. 知识整理模式名称更清晰：快速维护、AI 深度整理、AI 会议整理；关闭普通模型时，深度与会议入口会明确置灰并提示。
5. 新增任务中心，可查看导入、知识整理等后台任务进度，并支持安全取消；PGLite 忙碌时也会引导前往任务中心。
6. 软件更新支持展示本版本更新说明，并写入自动更新元数据；软件修复页可查看升级前数据库备份清单。
7. 修复 Ollama 本地普通模型偶发空回答；企微助手增加客服微信二维码，方便扫码咨询。
8. GitHub 发布收敛为 Windows 安装包与自动更新链路，安装与升级更专注本机桌面场景。

# AGENTS.md

## 阅读顺序

1. AGENTS.md
2. CLAUDE.md
3.[text](项目管理/变更台账.md)，仅搜索最近五次更新记录，必要时才搜索其他记录
4. [text](项目管理/Bug修复台账.md)，仅搜索最近五次更新记录，必要时才搜索其他记录

Agent 不允许：

* 删除原始资料
* 覆盖 Wiki
* 批量修改知识库

## 常见入口

* 架构：CLAUDE.md
* 技能：skills/RESOLVER.md
* 按主题架构：docs/architecture/ 下对应的单篇文档，不默认通读整个目录
* 知识库：wiki/
* 原始资料：sources/

## 工作原则
* 不修改和删除原始资料
* 保持知识链接完整
* 不要读取 TODOS.md 和 CHANGELOG.md（GBrain 上游历史文档，与 PMBrain 日常使用和开发无关）

## 信任边界
* 用户知识库、Wiki 和原始资料属于受保护数据；项目代码升级、测试和打包不得隐式修改、清空或重建这些数据。
* 向量重建、批量知识关系回填及其他数据级变更必须由用户明确授权；默认只执行只读检查和项目代码修改。

## 前端设计原则
* 桌面端和GUI端力求简洁给小白用户使用的，CLI是高级用户使用的。

## 修改原则
* 原则的都是最小改动的修改方法，且每次都要考虑是否与原版Gbrain的实现逻辑冲突，原则是还是以Gbrain的底层逻辑为准，我改变的更多只是GUI和桌面端的交互方式。
* 修改项目是要充分考虑实际已有用户使用的情况，基本原则是只改动项目，**禁止修改、删除和损坏用户的知识库数据和原始数据。修改要兼容老用户的使用。**
* 修改 Admin Console 后，必须打开当前工作区最新源码对应的页面。不得用正在运行的旧安装版、旧 Sidecar 或旧内嵌资源代替，如果是修改桌面端或者搜索、导入等操作不需要给我链接。
* 最后一步执行打包bun run build:win由用户完成，你只需要把需要打包的资源整理好就行。
* 修改代码时，如果涉及到改项目底层架构的修改一定要与用户确认后再执行，不要用户提出需求就马上改代码。**最底层：核心能力 / 数据逻辑层**也就是 CLI、GUI、桌面端共同调用的底层逻辑，这层为底层架构
* CLI修改要慎重，先确定原来有没有这个能力，如果修改会不会影响到其他能力。
* GUI / Admin / Desktop 原则上复用现有核心能力，不得为了界面需求复制一套业务逻辑。新增能力前先确认 Core / Operation / CLI / Admin API 是否已有可复用实现；若需要新增底层公共能力，先向用户确认。
* 项目按照规划执行时，如果执行完成后有与规划不一致的地方要列出来告诉用户。
* PMBrain的手机端UI暂不考虑，之前设计的移动端也不动了，先把电脑端做好前端适配就行。
* 用户要求查看、拉取、合并原版Gbrain的代码时，读取[[PMBrain与原版GBrain的检索和Dream功能对比.md]]内容，按照要求进行原版代码的合并处理。

## 测试原则
* 每次修改完成后按实际改动范围运行必要的定向测试；发现跨模块影响时逐步扩大相关测试范围，只有用户明确要求时才运行本地全量测试。
* 新功能先写测试脚本，完成编码后按照脚本进行测试。
* 首次写的测试脚本要用产品经理能听得懂的话描述出来你是要如何测试的。
* 最终提交Github进行的CI测试也要确保正常，才算最终测试完成。
* 设计修改到数据库范围的内容，要同时验证PGLite和Postgres

## 编码原则

- 向用户解释某些内容时，言简意赅、直截了当、坦诚直率。质疑站不住脚的假设，区分已证实的事实和不确定性。
- 在权威、最新的资料来源中进行实地调研，并将重要证据联系起来
- 遵守原有目标和限制条件；彻底完成已授权的工作，并在宣布完成前核实实际结果。
只有当决定存在实质性歧义、风险较高或需要审批时才提出问题
- 运用相关技能；仅在真正需要独立工作时才派遣子代理，并综合分析他们的发现。
- 保持变更简洁明了。避免无关的编辑、不必要的抽象和低效的测试。
- 测试可观察的行为，审查实质性变更，并在适用情况下验证实际界面中面向用户的功能。
- 保留无关的工作内容，并且绝不能进行超出用户授权范围的破坏性、生产性或外部操作。
- 报告有意义的障碍、结果和证据，避免无关紧要的进展。
- 若出现接口调不通、数据异常等问题，不允许擅自改成其他替代功能、新增无关兜底逻辑，仅保留原生报错或静默状态即可，除非我明确下达修改指令，否则不得擅自变更原有业务流程与功能设计。

### 5. 项目管理记录

**按子项目维护中文记录，不建立仓库级总台账。**

当某个子项目需要记录项目管理信息时：
- 如果该子项目内还没有 `项目管理` 文件夹，先创建该文件夹。
- 使用 `变更台账.md` 记录用户明确要求的项目变更。
- 使用 `Bug修复台账.md` 记录用户明确要求的 Bug 修复。
- 台账内容使用中文记录，且采用倒序的方式，最新的记录在最前面。

每条变更或 Bug 修复记录必须包含：
- 时间、版本号、标题、描述、是否完成、最终结果。
- 内容保持简洁、客观、可追溯。
- 工作完成或结果变化后，及时更新对应记录。

如果子项目存在明确版本号（如 `package.json`、`pyproject.toml`、`Cargo.toml`、应用内 VERSION 文件等），记录项目变更或 Bug 修复时必须同步递增版本号：
- 默认只递增最后一位版本号。例如当前 `1.0.0`，完成一次变更或 Bug 修复后更新为 `1.0.1`。
- 最后一位递增到 `99` 后，自动向前一位进位，并将最后一位归零。例如 `1.0.99` 的下一次默认更新为 `1.1.0`。
- 如果用户明确指定要递增前两位或指定具体版本号，优先遵循用户要求。
- 该规则只从新增规则之后的项目变更和 Bug 修复开始执行，历史台账和历史版本无需回改。

只有用户明确要求对子项目做项目变更或 Bug 修复时才记录。不要建立仓库级通用总台账。

---

**这些准则生效的标志是：** diff 中不必要的修改更少，因过度复杂化导致的重写更少，澄清性问题在实施前而非错误发生后提出。

